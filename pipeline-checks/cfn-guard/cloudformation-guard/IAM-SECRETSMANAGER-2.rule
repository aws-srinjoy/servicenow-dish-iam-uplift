AWS::IAM::ManagedPolicy PolicyDocument.Statement.*.Action.* NOT_IN [secretsmanager:GetSecretValue] << IAM-SECRETSMANAGER-2 Check that only authorized principals are able to retrieve a secret value. Sensitive or confidential information (e.g., credentials) would be expected to be stored in AWS Secrets Manager. Ensuring only authorized access reduces the risk of unauthorized access.