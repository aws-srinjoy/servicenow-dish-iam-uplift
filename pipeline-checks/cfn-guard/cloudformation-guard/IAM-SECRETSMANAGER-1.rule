AWS::IAM::ManagedPolicy PolicyDocument.Statement.*.Action.* NOT_IN [secretsmanager:DeleteSecret] << IAM-SECRETSMANAGER-1 Check that DeleteSecret is appropriately scoped and for authorized principals. Deleting a secret makes the secret immediately inaccessible and could impact applications that depend on the secret.